{% extends 'BackendBundle::backend.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>Vehiculetype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehiculeType.id }}</td>
            </tr>
            <tr>
                <th>Lib</th>
                <td>{{ vehiculeType.lib }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vehiculetype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vehiculetype_edit', { 'id': vehiculeType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
