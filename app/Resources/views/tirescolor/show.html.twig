{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tirescolor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tiresColor.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ tiresColor.color }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tirescolor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tirescolor_edit', { 'id': tiresColor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
