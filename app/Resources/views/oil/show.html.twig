{% extends 'BackendBundle:Layout:backend.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>Oil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oil.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('oil_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('oil_edit', { 'id': oil.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
