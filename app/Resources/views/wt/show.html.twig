{% extends 'BackendBundle::backend.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>Wt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wt.id }}</td>
            </tr>
            <tr>
                <th>Lib</th>
                <td>{{ wt.lib }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('wt_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('wt_edit', { 'id': wt.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
