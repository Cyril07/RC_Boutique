{% extends 'BackendBundle::backend.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>Batterytype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ batteryType.id }}</td>
            </tr>
            <tr>
                <th>Lib</th>
                <td>{{ batteryType.lib }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('batterytype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('batterytype_edit', { 'id': batteryType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
